# ICRA 粉苹果流程图

从白杯案例改为 2026 年 9 月 8 日的粉苹果案例，保持完整眼动仪视角、真实 gaze 放大、地图定位和下排执行过程的两行三列版式。采用原始 fig_story 的细边框、衬线字体和图下短说明。

## 文件

- `../Figures/fig_story_pink_apple_icra.pdf`：论文通栏 PDF，7.16 × 3.34 英寸，字体嵌入。
- `../Figures/fig_story_pink_apple_icra.png`：600 dpi，4296 × 2003。
- `../Figures/fig_story_pink_apple_icra.svg`：自包含可编辑 SVG，全部图片已嵌入。
- `figure.tex`：独立插图与英文图注；当前正文 `../EXPERIMENTS_0909.tex` 保留 `fig:story` 标签并引用新版图。
- `source_manifest.json`、`verification.json`：数据来源、测量坐标、画面时刻及导出检查。
- `case_text_zh.md`：粉苹果案例正文；已同步至当前中文定稿。

`../bundle_0909/Figures/` 中同步保存三种格式，bundle 内的当前英文与中文实验稿也已更新。原始双案例 `fig_story` 和拆分的 hand/box 图仍作为历史版本保存。

## 数据与标注

源录像：`/home/liuchy/recordings/2026_09_08/008/world.mp4`。
源会话：`/home/liuchy/Project/Intension/logs/20260908-010003/`。
粉苹果为地图实例 66（图中 apple 2），红苹果为实例 60（apple 1），与原始 fig_story 的编号习惯一致。

本段真实指令是 **“Grab this apple.” → “Put it in the box.”**。抓取请求 `20260908-010003-001` 和放置请求 `20260908-010003-002` 均有 `done` 记录。此次图中最后一步是入箱，未沿用白杯案例的递送结尾。该案例包含不同颜色的两个苹果实例，不再描述“两个同款白杯相隔 17 cm”。

(a)(b) 选用第 299 帧，时间戳 462749.398495，距首帧 10.021535 s，处于语音指代短语附近。(a) 保留原始鱼眼世界相机全视野；(b) 将同一帧去畸变后局部放大。黄色十字来自该帧前后 24 ms、置信度不低于 0.6 的 12 个原始 gaze 样本的中位值：

- 原始像素坐标：[987.3245, 507.0295]。
- 去畸变后：[987.5546, 507.0025]。

这些坐标未施加手动位移。十字符号为印刷放大，中心保持在真实落点。白色 ROI 矩形为放大区域指示，因 (b) 去畸变，边界与原始鱼眼图不宣称逐像素一致。绿色和白色实拍框为实例说明标注，不是检测器输出。

(c) 使用同一请求期间已记录的 fixation 462749.2455135–462749.5205005：

- 眼动仪位置：[0.824995, -1.407998, 1.670615] m。
- 三维注视落点：[0.656935, 0.873160, 0.846156] m。

射线端点使用该 fixation 的实际日志，未移到苹果中心。二维 raw gaze 和三维在线校准、聚合后的 fixation 是不同数据表示，图注已说明。视锥位置使用日志记录，朝向使用选中帧的离线 ArUco/PnP；15 个已知标记、60 个内点角点，平均归一化重投影误差约 0.001389，复算位置与日志位置相差约 7.6 mm。视锥大小仅用于表示朝向，不是完整标定视场。地图中的苹果框由保存的三维包围盒投影生成。

下排主画面分别为录像约 28.0 s、36.0 s、51.5 s，机器人 RGB 分别为 15.6 s、36.0 s、51.5 s。(d) 的小窗明确标为 **Early robot RGB**：此时机器人看不到目标，但它与主画面不是同一时刻；(e)(f) 使用相同标称时刻的两个视角。机器人 RGB 使用已核对的对齐视频，其原始屏幕录像时间为主录像时间减 15.4 s。

此次只替换案例图，未修改原始数据、白杯版本或 38 次任务的统计。

## 编辑与复现

SVG 是不依赖本机素材路径的可编辑交付文件。PDF 使用嵌入字体，照片和地图为原始素材的栅格图，文字与标记为矢量。

数据级复现需要来源说明中记录的原始录像、眼动日志和 3DGS checkpoint；这些本机原始文件未打包进本目录。构建脚本保留在生成工作区 `/home/liuchy/文档/ChatGPT/3dgs/story_work/build_icra_apple_story.py`，眼动提取与地图渲染脚本为同目录的 `prepare_apple_gaze_evidence.py`。
