# 测试方案:Intension 指令层 → 全链 demo(2026-07-29)

> 用法:层 0 每次改码必跑;层 1/2 变更交互后过一遍;层 3 上真机前全家族抽测。
> 纪律:**E1 语义用例(A2/B 族)永不改动**;负例必须干净失败(宁可不动);
> 现场踩到新 bug,先补用例进 run_regression.sh 再修(R8/R9 就是这么来的)。

## 0. 四层怎么跑

| 层 | 命令 | 测什么 |
|---|---|---|
| 0 自动回归 | `./Intension/run_regression.sh`(R1-R9,~40s) | 改码后语义没坏 |
| 1 假狗在环 | 终端1 `dog_link.py --fake`;终端2 `brain.py --skill-endpoint tcp://127.0.0.1:5583`;要视线再开 `gaze_live.sh --replay <录像> --publish 5581` | 交互体验/确认门/忙拒 |
| 2 真眼镜干跑 | `gaze_live.sh --publish 5581` + `brain.py`(缺省只打印) | 指代消解不动真狗 |
| 3 真狗 | `brain.py --skill-endpoint tcp://192.168.123.164:5583` | 全链 |

说法一律用当前地图已命名物体(names.json 活文件,以实际为准):
Bottle / orange / apple / cup / 物品台 / 狗桌边 / 黄色机器人 / My Keyboard / 机械臂…
新说法首次过 LLM(~2-5s)后进 parse_cache_v2,demo 台词提前预热一遍。

## A|槽位网格(功能主干)

| ID | 说法(例) | 前置 | 预期 | 状态 |
|---|---|---|---|---|
| A1 | 拿这个 | 盯物体 ≤4s 内 | 抓注视物,送回(缺省送回) | ✅R1 变体 |
| A2 | 把这个杯子拿来 | **两只同款杯**,盯左/右 | 类别过滤+视线选对那只(E1 核心,demo 物证) | ✅单杯 R1;双杯待地图 |
| A3 | 拿一下Bottle | 不需注视 | 名字→地图质心→抓取送回 | 手动 |
| A4 | 拿个杯子(泛指没看) | 4s 内没看杯 | 唯一同类→名字兜底;多只→拒绝并列候选 | 待自动化 |
| A5 | 去这里 / 去那边 | 盯某处家具/物体 | 纯导航到落点前 0.6m | ✅R4 |
| A6 | 去狗桌边 / 过来 | — | 名字导航 / 到用户身前朝向用户 | 手动 |
| A7 | 去这里拿orange | 盯目标处家具 | 导航注视处→到位检测→送回 | ✅R2/R9 |
| A8 | 去狗桌边拿Bottle | — | 名字地点→到位检测→送回(真机已通过链) | 手动 |
| A9 | 把这个拿到物品台那边 | 盯物体 | 视线选物+送物品台,**不送**用户 | ✅R5 |
| A10 | 把Bottle拿到那边去 | 盯**目的地** | 对称 case:名字选物+注视点送达 | 待自动化 |
| A11 | 拿着这个别动 | 盯物体 | 抓住不送(无 deliver_to) | 待自动化 |
| A12 | 去这里拿orange回来 | 盯地点 | 与缺省送回同义;防"回来"被解析歪(预热缓存) | 待自动化 |

(设计边界:「去XX拿这个」不进测试集——"拿这个"已由视线定物,地点冗余;系统恰好支持,不测。)

## B|消歧与绑定时序(E1/E4 素材,最值钱)

| ID | 场景 | 预期 | 状态 |
|---|---|---|---|
| B1 | 看 A 再看 B,说"拿这个" | 选 B(最近优先) | 待自动化(造双物体流) |
| B2 | 盯 A 移开视线,≤4s 内说 | 仍是 A(回看窗) | ✅R1 即此(106.5s 时在盯) |
| B3 | 盯 A 后隔 >4s 才说 | 拒绝/类别兜底(窗外)——先看后说的边界 | 待自动化 |
| B4 | 边走边盯再说(VOR) | 仍命中(世界系聚类卖点) | 层 2 真眼镜 |
| B5 | bias 戳过期前后对比(久不扫 tag) | 漂移被戳协议兜住 | 层 2,配 tag 协议 |

注意:B 族当前用打字测,t_word=回车时刻;上语音(词级时间戳)后窗口对齐会变,先用打字把语义验干净。

## C|流程与安全

| ID | 场景 | 预期 | 状态 |
|---|---|---|---|
| C1 | 确认门:y / n / 10s 不答 | 派发 / 取消 / 超时取消 | 手动(层1) |
| C2 | 确认中直接说新指令 | 旧提议取消,新指令生效 | 手动 |
| C3 | 狗执行中下新单 | "本单作废"提示,不排队;停→重下生效 | ✅R7 |
| C4 | 停:导航中/抓取中/送回中 | 即时停(不过 LLM);**抓着东西爪不松** | ✅R6(空闲);三阶段待自动化 |
| C5 | 连续链 去A→拿B→过来 | 全程零手动重置 | 手动 |

## D|负例(必须干净失败;对应 E2 预注册失败分类,见 §F)

| ID | 场景 | 预期 | 状态 |
|---|---|---|---|
| D1 | 盯地板说"去这里" | 明确拒绝(地板不算地点) | ✅R3 |
| D2 | 没看任何东西说"拿这个" | 提示"先看后说" | 待自动化 |
| D3 | 拿手机(地图没有) | 没有唯一命中+候选列表,不派发 | 待自动化 |
| D4 | 闲聊("今天真热") | help,绝不派发 | 待自动化 |
| D5 | 物名不在 detect_names 词表 | 「按原名直发」警告(名字映射缝早警) | 待自动化 |
| D6 | LLM 槽位错标(指代词进 query 等) | 卫生层清洗后仍走对(实测坏例回放) | ✅R8/R9 |

## E|跨房与视野外(仅真机,E2/E3 支柱)

| ID | 场景 | 预期 |
|---|---|---|
| E1 | 狗在房间2,人房间1:"拿一下XX" | 跨房导航→取→送回(E2 主干) |
| E2 | 同任务,XX 在/不在狗视野内对照 | 成功率无差(E3 不变性) |
| E3 | 设计边界:指代类限同房 | 视线够得着才有落点;跨房必须名字——正是"地图中介 vs 视野依赖"的论据 |
| E4 | 送达时用户已走动 | 送到确认时刻位置(已知简化,记录行为即可) |

## F|失败分类对照(E2 预注册,失败必归一类)

定位空窗→B5/D2 | 选错相邻实例→A2/B1 | 绑定窗错过→B3 | ASR错→(语音后)D 族扩 |
导航失败→E1+导航设计(REDESIGN §7) | 抓取失败→层3 | 通信超时→C3/dispatch 2s 超时

## G|待自动化清单(按性价比)

1. B1/B3(双物体+时序流:扩 gen_fake_gaze 支持场景参数)
2. A4/A10/A11/A12/D2/D3/D4(单条 script 用例,预热缓存即可)
3. C4 三阶段急停(假狗在环,定时发 stop)
4. A2 双杯(等下次重建把两只同款杯入图并命名)
